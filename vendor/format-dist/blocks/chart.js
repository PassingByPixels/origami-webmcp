import { validateChartData } from '../chart-data.js';
export const chartBlock = {
    key: 'chart',
    name: 'Chart (block)',
    schemaComment: [
        'an IN-SLIDE block, not a slide kind — any number may appear on any slide',
        'shape: <figure class="o-chartfig anim"> holding ONE inert <script type="application/json" data-odata="chart"> block,',
        '  then <div class="o-chart" data-chart-mount></div> (the runtime renders the SVG here), then an editable <figcaption>',
        'JSON shape: { type: bar|line|pie|timeseries|scatter|waterfall|boxplot|radar|gauge|heatmap|treemap|sankey, labels: ["Q1",…] (1-24), series: [{name, color:"#hex", values:[n per label], dash?:bool, markers?:bool}] (1-6; a heatmap takes 1-24), yMax: number|null, sliceColors?: ["#hex" per label] }',
        'optional presentation fields: showValues? (label every bar/point), title?/subtitle? (drawn above), xTitle?/yTitle? (bar/line axis titles); per-series dash?/markers? style the line (markers default on)',
        'bar layout: orientation?:horizontal (rank rows down the y-axis, up to 60 labels), barMode?:overlaid (nest bars per category) or stacked (one bar per category, series stacked; the axis scales to the stack total), highlightIndex?:int (shade one category)',
        'line extras: per-series fill?:bool draws a gradient area under the stroke; chart-level spark?:bool renders a SPARKLINE — no axes/ticks/legend/title, autoscaled to its own min-max',
        'timeseries: continuous numeric X — each series carries xs:[n] parallel to values (own length, so a series may stop early); no labels needed; draws left-to-right in Present',
        'scatter: numeric X and Y — each series carries xs:[n] parallel to values (y may be NEGATIVE), no labels needed; sizes?:[n] per point makes it a bubble (area-proportional radius), pointLabels?:[str] captions the points, quadrant?:{x,y,corners?:[tl,tr,bl,br]} adds the split lines',
        'timeseries and scatter cannot link to a ledger (the range→series mapper only emits bar/line/pie)',
        'pie takes exactly one series; slice colours default to a rotating palette per label, or pin them with sliceColors (one #hex per label)',
        'pie variants: donut?:bool adds an inner ring and a centre TOTAL; rose?:bool makes equal-angle wedges whose RADIUS encodes the value (area-honest, 4x value = 2x radius); both may be set together',
        'bar shapes (each exactly one series, and each rejects orientation/barMode/highlightIndex/histogram/pareto): funnel?:bool draws tapering stages, always named and valued, so showValues is rejected; polar?:bool draws categories as angular sectors on polar axes, radius = value',
        'radar: labels are the SPOKES (min 3), one series per shape; maxes?:[n per spoke] pins each spoke ceiling, absent = every spoke shares yMax or the data peak; showValues prints each vertex value',
        'gauge: exactly one label (the dial name) and one series of one value; gaugeMin?/gaugeMax? bound the dial (default 0-100, max > min, may be negative), unit?:str suffixes the centre readout; a gauge cannot link to a ledger and has no showValues',
        'heatmap: a GRID — labels are the COLUMNS, each series is one ROW (series.name is the row label, 1-24 rows), and every cell is coloured by its value on a sequential viridis ramp anchored at zero; yMax raises the ramp ceiling (extend-only), the value is printed IN each cell that has room for it so showValues is rejected, and a colour scale is drawn beside the grid; it CAN link to a ledger and is the only picture that uses the whole range rectangle, though the shared range→series mapper still reads at most 6 columns, so a linked heatmap gets at most 6 rows (the ledger preview marks the columns it is not reading)',
        'hexbin: chart-level hexbin?:bool on a SCATTER bins the point cloud into a pointy-top hexagonal lattice coloured by COUNT (24 Hexagonal binning); hexBins?:int 4-60 sets how many hex columns span the plot (default 20), showValues prints the count in each cell that fits one; it replaces the point marks, so sizes/pointLabels/quadrant are rejected alongside it',
        'treemap: a TREE of nested areas — exactly one series, and chart-level parents?:[int per label] holds the shape (-1 = a root, absent = every node a root); it must form a FOREST (no self-parent, no cycle) and a node that is another node\'s parent carries value 0, because a branch\'s size is the total of the leaves under it; up to 60 nodes, values >= 0, name and value printed in every cell that fits them so showValues is rejected, no x/y axis so xTitle/yTitle are rejected, and it cannot link to a ledger (the range→series mapper has no parent column)',
        'treemap variants (mutually exclusive): sunburst?:bool draws the same tree as a POLAR partition, one ring per depth with angle proportional to the subtree total (20 Sunburst); convex?:bool rounds the cell corners and insets each cell by a gap (25 Convex treemap)',
        'sankey: a FLOW GRAPH — chart-level links?:[{from:int,to:int,value:n}] over the labels (27 Sankey), from/to are label INDICES, value > 0, no self-loops, and the directed graph must be ACYCLIC; every node must appear in at least one link; up to 60 nodes and 120 links; exactly one series whose values are ALL ZERO, because a node is sized by its THROUGHPUT (the larger of its in-sum and out-sum), which the links already state; name and throughput are printed beside each node bar wherever there is room so showValues is rejected, no x/y axis so xTitle/yTitle are rejected, and it cannot link to a ledger (a flow is a pair of nodes and a magnitude, which the range→series mapper has no column for)',
        'radar, gauge, funnel, polar, treemap and sankey have NO x/y axis — xTitle/yTitle are rejected on all six',
        'plotHeight?:number 180-1200 sets the PLOT BOX height in viewBox units (default 318, or 336 on a treemap/sankey) — it gives the marks more room at the SAME text size, and the title/axis bands still grow the viewBox around it; accepted on every type but only honoured on the fixed-height ones (a horizontal bar and a heatmap are rowCount x pitch, a sparkline has no plot box), and on a pie/radar/gauge/radial bar the disc stops growing past the plot WIDTH',
        'when editing the JSON keep every "<" escaped as \\u003c — never emit a raw "<" inside the block',
    ],
    data: { placement: 'block', validate: validateChartData },
};
