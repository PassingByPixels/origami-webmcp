/* /charts/ — the shell scoped to ONE chart or Venn block (docs/SITE.md, "Mini tools"). */
import { CHARTS_MODE } from '../core/modes.js';
import { bootShell } from './shell.js';

bootShell(CHARTS_MODE);
